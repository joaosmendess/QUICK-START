import { createTheme } from '@mui/material/styles';

const theme = createTheme({
 
    typography: {
    fontFamily: 'Poppins, sans-serif',
  },
  
  // Você pode adicionar outras personalizações aqui
});

export default theme;
